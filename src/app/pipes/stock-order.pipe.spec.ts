import { StockOrderPipe } from './stock-order.pipe';

describe('StockOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new StockOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
